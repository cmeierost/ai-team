the tool info should be placed at the implementation of the tool.. not just somewhere else.
This way we can easily find the tool info when we are looking at the tool implementation.
also the commands (tools callable by the human) should be placed in the same place as the tool info. This way we can easily find the commands when we are looking at the tool implementation.