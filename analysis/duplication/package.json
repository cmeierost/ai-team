{
  "name": "@aspect/duplication",
  "version": "0.1.0",
  "description": "Code duplication detection and metrics calculator",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "aspect-fuzzy-dup": "dist/cli/fuzzy-dup.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests",
    "fuzzy": "node dist/cli/fuzzy-dup.js",
    "clean": "rm -rf dist"
  },
  "license": "MIT",
  "dependencies": {
    "@aspect/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}