{
  "name": "@aspect/dead-code",
  "version": "0.1.0",
  "description": "Dead code detector — finds unused classes, interfaces, methods, and re-exports",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "license": "MIT",
  "dependencies": {
    "@aspect/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
